version: "1.0"
namespace: keeper.git.migrations

entries:
  # keeper.git.migrations:01_create_runs
  - name: 01_create_runs
    kind: function.lua
    meta:
      type: migration
      comment: Single keeper_git_runs table — one row per cluster index build, snapshot lives in payload_json
      target_db: keeper.state:db
      timestamp: "2026-04-27T00:00:00Z"
    source: file://01_create_runs.lua
    imports:
      migration: wippy.migration:migration
    method: migrate
    