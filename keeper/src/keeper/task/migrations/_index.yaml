version: "1.0"
namespace: keeper.task.migrations

entries:
  # keeper.task.migrations:01_create_schema
  - name: 01_create_schema
    kind: function.lua
    meta:
      type: migration
      comment: Create keeper task schema — keeper_tasks (lifecycle) + keeper_task_nodes (every event / payload, typed). One hierarchical log.
      target_db: keeper.state:db
      timestamp: "2026-04-24T12:00:00Z"
    source: file://01_create_schema.lua
    imports:
      migration: wippy.migration:migration
    method: migrate
    