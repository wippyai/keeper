# Keeper Usage

Keeper Usage adds usage analytics pages and APIs on top of `wippy/usage`.

The module is intentionally separate from `keeper/keeper` so deployments can
install or remove usage analytics independently.

