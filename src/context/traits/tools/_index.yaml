version: "1.0"
namespace: keeper.context.traits.tools

entries:
  # keeper.context.traits.tools:tools_aware
  - name: tools_aware
    kind: registry.entry
    meta:
      type: agent.trait
      title: Tools Aware
      comment: Inject available tools into system prompt
      tags:
        - tools
        - context
        - discovery
      icon: tabler:tools
    prompt_func_id: keeper.context.traits.tools:tools_list
    
  # keeper.context.traits.tools:tools_list
  - name: tools_list
    kind: function.lua
    meta:
      comment: List available tools for prompt injection
      output_schema: |
        {
          "type": "string"
        }
    source: file://tools_list.lua
    modules:
      - registry
    method: handler
    