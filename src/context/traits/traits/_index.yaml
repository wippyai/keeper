version: "1.0"
namespace: keeper.context.traits.traits

entries:
  # keeper.context.traits.traits:traits_aware
  - name: traits_aware
    kind: registry.entry
    meta:
      type: agent.trait
      title: Traits Aware
      comment: Inject available traits into system prompt
      tags:
        - traits
        - context
        - discovery
      icon: tabler:puzzle
    prompt_func_id: keeper.context.traits.traits:traits_list
    
  # keeper.context.traits.traits:traits_list
  - name: traits_list
    kind: function.lua
    meta:
      comment: List available traits for prompt injection
      output_schema: |
        {
          "type": "string"
        }
    source: file://traits_list.lua
    modules:
      - registry
    method: handler
    