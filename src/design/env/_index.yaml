version: "1.0"
namespace: keeper.design.env

entries:
  # keeper.design.env:database_resource
  - name: database_resource
    kind: env.variable
    meta:
      comment: Database resource for design workspace storage
      icon: tabler:database
      private: true
    default: app:db
    storage: app.env:router
    variable: DESIGN_DATABASE_RESOURCE
    