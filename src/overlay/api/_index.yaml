version: "1.0"
namespace: keeper.overlay.api

entries:
  # keeper.overlay.api:add_view_permission
  - name: add_view_permission
    kind: function.lua
    meta:
      comment: Add view permission for workspace access
      description: Workspace permission management HTTP endpoints
    source: file://add_view_permission.lua
    modules:
      - http
      - json
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:create_workspace
  - name: create_workspace
    kind: function.lua
    meta:
      comment: Create new workspace for user
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://create_workspace.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:create_workspace.endpoint
  - name: create_workspace.endpoint
    kind: http.endpoint
    meta:
      comment: Create workspace endpoint
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: POST
    func: create_workspace
    path: /workspaces
    
  # keeper.overlay.api:create_workspace_context
  - name: create_workspace_context
    kind: function.lua
    meta:
      comment: Create new workspace context
      description: Workspace context management HTTP endpoints
    source: file://create_workspace_context.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:create_workspace_context.endpoint
  - name: create_workspace_context.endpoint
    kind: http.endpoint
    meta:
      comment: Create workspace context endpoint
      description: Workspace context management HTTP endpoints
    method: POST
    func: create_workspace_context
    path: /workspaces/{id}/context
    
  # keeper.overlay.api:create_workspace_permission
  - name: create_workspace_permission
    kind: function.lua
    meta:
      comment: Create permission for workspace namespace access
      description: Workspace permission management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://create_workspace_permission.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:create_workspace_permission.endpoint
  - name: create_workspace_permission.endpoint
    kind: http.endpoint
    meta:
      comment: Create workspace permission endpoint
      description: Workspace permission management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: POST
    func: create_workspace_permission
    path: /workspaces/{id}/permissions
    
  # keeper.overlay.api:create_workspace_review
  - name: create_workspace_review
    kind: function.lua
    meta:
      comment: Create new workspace review
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://create_workspace_review.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:create_workspace_review.endpoint
  - name: create_workspace_review.endpoint
    kind: http.endpoint
    meta:
      comment: Create workspace review endpoint
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: POST
    func: create_workspace_review
    path: /workspaces/{id}/reviews
    
  # keeper.overlay.api:delete_workspace
  - name: delete_workspace
    kind: function.lua
    meta:
      comment: Delete workspace and all entries
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://delete_workspace.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:delete_workspace.endpoint
  - name: delete_workspace.endpoint
    kind: http.endpoint
    meta:
      comment: Delete workspace endpoint
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: DELETE
    func: delete_workspace
    path: /workspaces/{id}
    
  # keeper.overlay.api:delete_workspace_context
  - name: delete_workspace_context
    kind: function.lua
    meta:
      comment: Delete workspace context
      description: Workspace context management HTTP endpoints
    source: file://delete_workspace_context.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:delete_workspace_context.endpoint
  - name: delete_workspace_context.endpoint
    kind: http.endpoint
    meta:
      comment: Delete workspace context endpoint
      description: Workspace context management HTTP endpoints
    method: DELETE
    func: delete_workspace_context
    path: /workspaces/{id}/context/{context_id}
    
  # keeper.overlay.api:delete_workspace_entry
  - name: delete_workspace_entry
    kind: function.lua
    meta:
      comment: Delete entry from workspace
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://delete_workspace_entry.lua
    modules:
      - http
      - security
    imports:
      workspace_session: keeper.overlay:workspace
    method: handler
    
  # keeper.overlay.api:delete_workspace_entry.endpoint
  - name: delete_workspace_entry.endpoint
    kind: http.endpoint
    meta:
      comment: Delete workspace entry endpoint
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: DELETE
    func: delete_workspace_entry
    path: /workspaces/{id}/entries/{entry_id}
    
  # keeper.overlay.api:delete_workspace_permission
  - name: delete_workspace_permission
    kind: function.lua
    meta:
      comment: Delete workspace permission
      description: Workspace permission management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://delete_workspace_permission.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:delete_workspace_permission.endpoint
  - name: delete_workspace_permission.endpoint
    kind: http.endpoint
    meta:
      comment: Delete workspace permission endpoint
      description: Workspace permission management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: DELETE
    func: delete_workspace_permission
    path: /workspaces/{id}/permissions/{permission_id}
    
  # keeper.overlay.api:delete_workspace_review
  - name: delete_workspace_review
    kind: function.lua
    meta:
      comment: Delete workspace review
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://delete_workspace_review.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:delete_workspace_review.endpoint
  - name: delete_workspace_review.endpoint
    kind: http.endpoint
    meta:
      comment: Delete workspace review endpoint
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: DELETE
    func: delete_workspace_review
    path: /workspaces/{id}/reviews/{review_id}
    
  # keeper.overlay.api:get_workspace
  - name: get_workspace
    kind: function.lua
    meta:
      comment: Get complete workspace details with entries
      description: Workspace management HTTP endpoints
    source: file://get_workspace.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:get_workspace.endpoint
  - name: get_workspace.endpoint
    kind: http.endpoint
    meta:
      comment: Get workspace endpoint
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: get_workspace
    path: /workspaces/{id}
    
  # keeper.overlay.api:get_workspace_context
  - name: get_workspace_context
    kind: function.lua
    meta:
      comment: Get workspace context by ID
      description: Workspace context management HTTP endpoints
    source: file://get_workspace_context.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:get_workspace_context.endpoint
  - name: get_workspace_context.endpoint
    kind: http.endpoint
    meta:
      comment: Get workspace context endpoint
      description: Workspace context management HTTP endpoints
    method: GET
    func: get_workspace_context
    path: /workspaces/{id}/context/{context_id}
    
  # keeper.overlay.api:get_workspace_entry
  - name: get_workspace_entry
    kind: function.lua
    meta:
      comment: Get entry details with all views (merged, original, workspace)
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://get_workspace_entry.lua
    modules:
      - http
      - security
    imports:
      workspace_session: keeper.overlay:workspace
    method: handler
    
  # keeper.overlay.api:get_workspace_entry.endpoint
  - name: get_workspace_entry.endpoint
    kind: http.endpoint
    meta:
      comment: Get workspace entry endpoint
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: get_workspace_entry
    path: /workspaces/{id}/entries/{entry_id}
    
  # keeper.overlay.api:get_workspace_review
  - name: get_workspace_review
    kind: function.lua
    meta:
      comment: Get specific workspace review
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://get_workspace_review.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:get_workspace_review.endpoint
  - name: get_workspace_review.endpoint
    kind: http.endpoint
    meta:
      comment: Get workspace review endpoint
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: get_workspace_review
    path: /workspaces/{id}/reviews/{review_id}
    
  # keeper.overlay.api:list_workspace_contexts
  - name: list_workspace_contexts
    kind: function.lua
    meta:
      comment: List workspace contexts with filtering and pagination
      description: Workspace context management HTTP endpoints
    source: file://list_workspace_contexts.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:list_workspace_contexts.endpoint
  - name: list_workspace_contexts.endpoint
    kind: http.endpoint
    meta:
      comment: List workspace contexts endpoint
      description: Workspace context management HTTP endpoints
    method: GET
    func: list_workspace_contexts
    path: /workspaces/{id}/context
    
  # keeper.overlay.api:list_workspace_ops
  - name: list_workspace_ops
    kind: function.lua
    meta:
      comment: List workspace operations audit trail
      description: Workspace operations audit HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://list_workspace_ops.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:list_workspace_ops.endpoint
  - name: list_workspace_ops.endpoint
    kind: http.endpoint
    meta:
      comment: List workspace ops endpoint
      description: Workspace operations audit HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: list_workspace_ops
    path: /workspaces/{id}/ops
    
  # keeper.overlay.api:list_workspace_permissions
  - name: list_workspace_permissions
    kind: function.lua
    meta:
      comment: List workspace permissions
      description: Workspace permission management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://list_workspace_permissions.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:list_workspace_permissions.endpoint
  - name: list_workspace_permissions.endpoint
    kind: http.endpoint
    meta:
      comment: List workspace permissions endpoint
      description: Workspace permission management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: list_workspace_permissions
    path: /workspaces/{id}/permissions
    
  # keeper.overlay.api:list_workspace_reviews
  - name: list_workspace_reviews
    kind: function.lua
    meta:
      comment: List workspace reviews
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://list_workspace_reviews.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:list_workspace_reviews.endpoint
  - name: list_workspace_reviews.endpoint
    kind: http.endpoint
    meta:
      comment: List workspace reviews endpoint
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: list_workspace_reviews
    path: /workspaces/{id}/reviews
    
  # keeper.overlay.api:list_workspaces
  - name: list_workspaces
    kind: function.lua
    meta:
      comment: List user's workspaces with filtering and pagination
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://list_workspaces.lua
    modules:
      - http
      - security
    imports:
      reader: keeper.overlay.persist:reader
    method: handler
    
  # keeper.overlay.api:list_workspaces.endpoint
  - name: list_workspaces.endpoint
    kind: http.endpoint
    meta:
      comment: List workspaces endpoint
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: GET
    func: list_workspaces
    path: /workspaces
    
  # keeper.overlay.api:reset_workspace_entry
  - name: reset_workspace_entry
    kind: function.lua
    meta:
      comment: Reset entry to main registry version
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://reset_workspace_entry.lua
    modules:
      - http
      - security
    imports:
      workspace_session: keeper.overlay:workspace
    method: handler
    
  # keeper.overlay.api:reset_workspace_entry.endpoint
  - name: reset_workspace_entry.endpoint
    kind: http.endpoint
    meta:
      comment: Reset workspace entry endpoint
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: POST
    func: reset_workspace_entry
    path: /workspaces/{id}/entries/{entry_id}/reset
    
  # keeper.overlay.api:update_workspace
  - name: update_workspace
    kind: function.lua
    meta:
      comment: Update workspace metadata and status
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://update_workspace.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:update_workspace.endpoint
  - name: update_workspace.endpoint
    kind: http.endpoint
    meta:
      comment: Update workspace endpoint
      description: Workspace management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: PUT
    func: update_workspace
    path: /workspaces/{id}
    
  # keeper.overlay.api:update_workspace_context
  - name: update_workspace_context
    kind: function.lua
    meta:
      comment: Update workspace context content and metadata
      description: Workspace context management HTTP endpoints
    source: file://update_workspace_context.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:update_workspace_context.endpoint
  - name: update_workspace_context.endpoint
    kind: http.endpoint
    meta:
      comment: Update workspace context endpoint
      description: Workspace context management HTTP endpoints
    method: PUT
    func: update_workspace_context
    path: /workspaces/{id}/context/{context_id}
    
  # keeper.overlay.api:update_workspace_review
  - name: update_workspace_review
    kind: function.lua
    meta:
      comment: Update workspace review content and metadata
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://update_workspace_review.lua
    modules:
      - http
      - security
    imports:
      writer: keeper.overlay.persist:writer
    method: handler
    
  # keeper.overlay.api:update_workspace_review.endpoint
  - name: update_workspace_review.endpoint
    kind: http.endpoint
    meta:
      comment: Update workspace review endpoint
      description: Workspace review management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: PUT
    func: update_workspace_review
    path: /workspaces/{id}/reviews/{review_id}
    
  # keeper.overlay.api:upsert_workspace_entry
  - name: upsert_workspace_entry
    kind: function.lua
    meta:
      comment: Update or create entry in workspace
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    source: file://upsert_workspace_entry.lua
    modules:
      - http
      - security
    imports:
      workspace_session: keeper.overlay:workspace
    method: handler
    
  # keeper.overlay.api:upsert_workspace_entry.endpoint
  - name: upsert_workspace_entry.endpoint
    kind: http.endpoint
    meta:
      comment: Update workspace entry endpoint
      description: Workspace entry management HTTP endpoints
      depends_on:
        - ns:keeper.overlay
        - ns:keeper.overlay.persist
    method: PUT
    func: upsert_workspace_entry
    path: /workspaces/{id}/entries/{entry_id}
    