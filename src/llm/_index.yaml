version: "1.0"
namespace: keeper.llm

entries:
  # keeper.llm:summarize
  - name: summarize
    kind: library.lua
    meta:
      comment: Tool-level output compression via cheap LLM (class:nano). Preserves identifiers, drops prose.
    source: file://summarize.lua
    modules:
      - logger
      - time
    imports:
      prompt: wippy.llm:prompt
      llm: wippy.llm:llm
    